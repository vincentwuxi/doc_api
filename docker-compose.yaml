version: '3.8'

services:
  docsight:
    build: .
    container_name: docsight
    ports:
      - "3070:80"
    restart: unless-stopped
    networks:
      - kreuzberg-deploy_default

networks:
  kreuzberg-deploy_default:
    external: true
